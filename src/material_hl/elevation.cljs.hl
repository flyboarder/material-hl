(ns material-hl.elevation
  (:require [material-hl.core :as core]))

(defmethod hoplon.core/do! :elevation
  [elem _ v]
  (let []
    (when v
      (hoplon.core/do! elem :css {(keyword (str "mdc-elevation--z" v)) true}))))
